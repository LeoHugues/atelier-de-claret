<form class="field has-addons" method="get" action="{{ path('search') }}" enctype="text/plain">
    <label class="label is-hidden" for="search">Search</label>
    <div class="control">
        <input class="input" type="search" value="{% if search is defined %}{{ search|escape }}{% endif %}" placeholder="Rechercher" name="search" id="search" style="width: 9rem;">
    </div>
    <div class="control">
        <button class="button is-link" type="submit">{{ __('action.search') }}</button>
    </div>
</form>